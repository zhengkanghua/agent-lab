# 公共任务组件与任务管理重构

状态：实施中（P1、P6 已完成；本地实现与离线验证、真实 PostgreSQL 迁移／交接／并发验收、文档 PostgreSQL／Qdrant 回归、三存储故障恢复，以及遗留纯计算的一次性交接修复均已通过验证；开发库已升级且用户数据核对一致。共用 Redis 配置、Windows 原生 HTTP／Beat／solo Worker 和真实页面联调已通过；三批提交已推送至 `1e5df62`，首次 CI 后端、前端和 Linux 验收通过，但日志发现 Beat 退出异常，部署停在候选编排上传权限，服务尚未切换；两项修复正在验证）。建立日期：2026-09-11。需求与架构依据为 ADR 0019 的已确认重构方向。

本 spec 是开发的施工规格与进度记录。准备工作已在 `087b4c3` 提交；2026-09-12 开始实施，期间逐项更新进度、验证结果和剩余问题。

## 问题陈述

老板希望项目新增业务领域时，都能使用同一套周期任务和一次性后台任务能力，不再为每个业务分别实现调度、提交、查询、重试和恢复。

当前已有任务类型注册、执行记录和写资源协调，但后台工作仍分散在独立 scheduler、API 进程和文档消费者中。手动 Pipeline 等待业务完成后才返回，任务管理主要围绕周期配置，配置修改、历史保留和异常恢复也沿用早期规则。这些边界不适合作为后续业务共同依赖的组件。

重构需要同时解决配置体验与执行可靠性：任务受理后不能因重启或 Redis 消息丢失而消失，也不能因重复请求或消息重新投递而重复开始业务。已有文档待办、完成标记、跨存储恢复和 UTC 存储约定必须继续发挥作用。

## 解决方案

建立项目内部公共任务组件，周期到点、人工触发和业务直接提交共用受理、执行记录、查询、重试及取消能力。业务开发者注册任务类型、参数和处理函数，业务状态与资源规则仍由所属业务管理。

采用 Celery Beat 负责周期触发，Celery Worker 承担后台执行，Redis 传递任务消息，PostgreSQL 保存配置、已受理执行、结果与恢复依据。已有三类定时任务、文档后台处理和 HTTP 手动 Pipeline 本次统一迁入 Worker。

后台入口改为“任务管理”，提供周期配置与任务执行两个视图。老板可以选择常用周期或填写 cron，查看等待原因和失败结果，执行“立即执行／重试”，取消尚未开始或等待重试的执行。批量恢复复用已有业务待办，只有无法确认安全继续时才提示核实。

## 用户故事

1. 作为业务开发者，我想要通过注册任务类型接入公共任务组件，以便新增业务时不用修改调度和执行核心。
2. 作为业务开发者，我想要直接提交一次性后台任务，以便没有周期需求的工作也能获得持久受理、查询和恢复能力。
3. 作为业务开发者，我想要让周期任务和一次性任务共用参数校验、处理函数与结果契约，以便相同工作不会出现两套执行逻辑。
4. 作为业务开发者，我想要继续由所属业务管理文档状态、资源占用和恢复依据，以便公共任务组件保持通用。
5. 作为超级用户，我想要从常用周期选项创建定时任务，以便减少填写 cron 的成本。
6. 作为超级用户，我想要直接填写五段式 cron，以便表达常用选项没有覆盖的执行周期。
7. 作为超级用户，我想要看到明确的调度时区和未来几次执行时间，以便在保存前核对任务会在什么时候触发。
8. 作为超级用户，我想要继续按北京时间配置任务，以便日常操作直观，同时数据库中的具体时间仍保持 UTC。
9. 作为超级用户，我想要立即触发一次定时任务，以便处理临时需要，而不必调整周期或等待下一次触发。
10. 作为超级用户，我想要提交后尽快取得任务执行编号，以便不必保持一个长时间等待的 HTTP 请求。
11. 作为超级用户，我想要在任务管理中查看周期配置与全部任务执行，以便一次性任务也能统一追踪。
12. 作为超级用户，我想要仅凭执行编号查询详情，以便周期配置删除或目标不在最近历史列表时仍能找到执行结果。
13. 作为超级用户，我想要离开页面后任务仍继续处理，并在重新进入时继续查询，以便页面生命周期不影响后台工作。
14. 作为超级用户，我想要同一次请求因超时重发时返回原执行编号，以便不会多创建一次任务执行。
15. 作为超级用户，我想要不同的操作即使参数相同也保持独立，以便系统不会错误合并我主动发起的两次工作。
16. 作为超级用户，我想要在任务启用或执行期间修改 cron 和参数，以便新配置用于之后受理的执行，原执行继续使用原快照。
17. 作为超级用户，我想要停用周期配置后仅停止未来周期触发，以便已经受理的工作及其允许的自动重试继续完成。
18. 作为超级用户，我想要删除周期配置时保留已受理执行和历史，以便停止后续调度不会丢失已有工作及排查依据。
19. 作为超级用户，我想要停机期间错过的周期直接跳过，以便恢复后不会集中补执行历史周期。
20. 作为超级用户，我想要已经持久受理但尚未开始的执行在重启后继续，以便“已受理”代表工作有可靠去向。
21. 作为超级用户，我想要同一周期配置最多保留一条未结束执行，以便上一条尚在排队或执行时不会继续堆积同类周期工作。
22. 作为超级用户，我想要等待自动重试仍算同一次未结束执行，以便下一周期不会绕过重叠限制。
23. 作为超级用户，我想要看到任务因业务资源占用而等待的原因，以便区分正常等待和故障。
24. 作为超级用户，我想要资源释放后任务继续执行且等待不消耗重试次数，以便正常竞争不会变成失败。
25. 作为业务开发者，我想要声明哪些错误允许安全自动重试，以便结果不确定的写操作不会被统一重试策略误处理。
26. 作为超级用户，我想要可安全重试的错误默认在初次尝试之外最多重试三次且间隔逐渐增加，以便短暂故障能恢复，又不会无限执行。
27. 作为超级用户，我想要重试耗尽后明确记录失败，以便根据失败原因决定是否人工重试。
28. 作为超级用户，我想要某次执行失败后下一正常周期仍可独立执行并重新计算重试额度，以便一次失败不会停用周期配置。
29. 作为超级用户，我想要人工重试沿用失败时参数、新建执行并关联原记录，以便保留两次执行的事实和排查线索。
30. 作为超级用户，我想要通过当前配置的“立即执行”使用最新参数，以便与使用旧参数的人工重试清楚区分。
31. 作为超级用户，我想要取消尚未开始或等待自动重试的执行，以便停止已经不需要的待处理工作。
32. 作为超级用户，我想要取消成功后旧消息不能再启动业务，而业务已经开始时明确提示不能取消，以便操作结果可信。
33. 作为超级用户，我想要已完整处理的批次成果在中断后保留，以便恢复只继续业务仍未完成的工作。
34. 作为超级用户，我想要系统根据已有业务依据自动处理能确认安全恢复的情况，以便普通失败不需要额外人工操作。
35. 作为超级用户，我想要无法确认旧执行者或远端写入已结束时看到核实原因和步骤，以便不会因盲目重试产生冲突写入。
36. 作为超级用户，我想要日常只使用任务详情和“立即执行／重试”，以便不必学习另一套独立恢复页面。
37. 作为超级用户，我想要现有同步、索引和清理任务统一在 Worker 执行，以便手动和周期触发具有一致的处理方式。
38. 作为超级用户，我想要 HTTP 手动 Pipeline 返回受理编号，再查询同步和处理统计，以便耗时业务不长期占用请求。
39. 作为超级用户，我想要文档解析、预览与采用继续由持久业务待办自动驱动，以便不需要额外创建定时任务。
40. 作为超级用户，我想要单篇资料处理异常仍保留原因且其他正常资料继续处理，以便迁移队列不会破坏现有文档处理体验。
41. 作为超级用户，我想要文档审核、正式版本可见性和清理资格保持原业务规则，以便后台执行方式变化不会改变数据的使用边界。
42. 作为超级用户，我想要普通已结束执行默认保留三十天并能调整默认天数，以便平衡排查需要和历史存储量。
43. 作为超级用户，我想要未结束、待核实、恢复所需和仍被重试关联的记录受到保护，以便历史清理不会破坏进行中的工作。
44. 作为超级用户，我想要普通详情过期后重发旧请求仍返回原编号并告知过期，以便历史清理不会意外触发重复工作。
45. 作为超级用户，我想要在网页调整默认重试次数、间隔和历史保留策略，以便日常策略调整不需要修改部署配置。
46. 作为超级用户，我想要策略修改只影响之后受理的执行，以便已受理执行及其自动重试保持原有规则。
47. 作为超级用户，我想要看到真实的等待、失败和业务统计，以便未知结果或部分失败不会被展示成“本轮无变更”或全部成功。
48. 作为账号使用者，我想要任务管理和业务执行查询继续遵守原有权限，以便公共任务接口不会扩大数据访问范围。
49. 作为部署维护人员，我想要独立配置 Redis 连接与 Worker 容量，以便后台工作与 API 请求相互隔离并可按需要扩容。
50. 作为部署维护人员，我想要区分调度停止、投递失败、Worker 不领取和业务等待，以便能定位任务停在哪里。
51. 作为部署维护人员，我想要 Worker 正常退出、强制中断和 Redis 重启具有可验证的恢复路径，以便生产行为不依赖理想的关停顺序。
52. 作为部署维护人员，我想要按明确顺序迁移并停用旧执行入口，以便新旧调度器和消费者不会同时处理同一工作。

## 实现决策

1. **公共职责与业务依赖。** 公共组件提供任务类型注册、受理、调度、投递、执行状态、操作和查询。业务提供参数模型、普通处理函数、结果摘要、可安全重试的错误分类，以及必要的恢复判断。通过装配注册，公共核心不依赖知识库或 Agent 的具体实现；不从数据库加载业务代码。前端业务表单与结果展示通过显式适配接入。

2. **数据身份与快照。** 保留 `ScheduledJob` 表达周期配置、`JobRun` 表达任务执行的概念。执行可不关联周期配置；关联过配置的执行保留稳定来源身份和必要快照，删除配置不级联删除执行。持久化受理参数、任务类型及必要版本、来源、操作主体、执行策略、状态、尝试次数、投递及可开始时间、领取身份、结果和恢复信息。新增或调整唯一约束以保护请求去重、同周期事件受理和同配置的未结束执行上限，不能只靠进程内检查。

3. **受理与消息可靠性。** 同一 PostgreSQL 事务保存执行、原请求去重回执及投递依据，事务成功后才返回已受理。执行记录自身承载投递状态，不增加独立通用消息平台。提交后尝试发布，Beat 维护循环有间隔地补投已到时间但尚未领取的执行；发布成功也不能成为永久停止核对的依据。Redis 消息只携带执行编号和投递代次，消息重发不增加业务尝试次数。Worker 按状态、代次及时间条件原子领取，迟到或重复消息不能再次开始业务，结果按领取代次条件写回。

4. **请求去重。** 去重身份包含提交主体、业务操作和请求标识。相同请求重发返回原编号；同一标识换请求内容返回冲突；不同标识不因参数相同合并。比较原请求内容，不能因周期配置后来修改而把原请求重发判成新操作。周期事件以稳定配置编号和计划 UTC 时刻识别，配置版本变化不产生同一时刻的第二个周期事件。

5. **Beat 与 cron。** 单个 Celery Beat 通过项目的 PostgreSQL 适配器读取动态配置，原子完成版本及启用复核、受理或记录重叠跳过、推进下一计划时刻。启动和恢复只安排未来周期，保留现有正常投递误差边界，不补停机期间的周期。现有 `CronTrigger` 仅保留为五段式 cron 计算依赖，校验、预览和调度共用相同计算入口；移除 APScheduler 调度器与 API 内调度来源。不能直接套用默认静态 Beat 配置或 Django 专用管理组件。

6. **配置并发与生效。** 修改、停用、删除和周期受理通过数据库事务协调先后顺序。允许启用或执行期间修改配置，之后受理的执行使用新配置，已经受理的执行及自动重试使用原快照。停用、删除只阻止未来周期，停用配置仍允许人工立即触发。同配置在排队、等待资源、执行中和等待重试期间占据同一个并发名额；新周期跳过，不同的人工请求明确返回冲突及当前执行编号。

7. **执行状态与 HTTP 契约。** 契约需区分排队、资源等待、业务执行中、等待自动重试、待核实，以及成功、失败、已取消和周期跳过；实际编码在后端 schema 与 OpenAPI 中统一，前端由此生成类型。受理接口返回 HTTP 202、执行编号及当前状态，不能把排队描述为业务已开始。提供独立于周期配置的执行列表、详情、取消和人工重试能力。HTTP 手动 Pipeline 改为同样的受理和查询方式，最终统计及业务错误通过执行详情表达；原参数校验、权限和错误脱敏继续有效。

8. **自动重试与人工操作。** 任务类型判断错误是否可安全重试，默认在初次尝试之外最多自动重试三次，间隔逐渐增加。次数和下次尝试时刻保存在 PostgreSQL，由 Beat 到期投递，不另外启用 Celery `autoretry` 形成第二套计数，也不把长期等待交给 Redis 中的 `ETA/countdown`。自动重试沿用原编号、参数和策略；人工重试使用原失败参数，新建执行并关联原记录，采用新受理时生效的策略。普通失败释放占用，耗尽只结束本次执行，下一正常周期有独立额度。

9. **资源等待与取消。** 业务资源正常占用时持久保存等待原因和再次检查时间，释放 Worker 执行位置，资源可用后继续同一次执行，不消耗业务重试次数。尚未开始业务的资源等待属于可取消范围，等待自动重试也可取消；正在进行的业务尝试不提供取消。取消与开始业务通过同一状态条件竞争，取消成功后旧消息失效。需要核实的旧占用不视为普通资源竞争，更不能因心跳过期自动解锁。

10. **异常恢复。** 从未开始的已受理执行补投；已开始的只有在业务持久依据、旧执行者状态及远端核验支持安全继续时才自动恢复，自动再次尝试仍受原额度约束。证据不足时保留待核实和写占用，详情说明阻塞原因及核实步骤，异常处置复用现有维护入口。业务结束但结果保存失败时，只重试保存或核对结果，不能直接重做业务。待核实不能按普通失败释放并发保护、启动下一次冲突写入或裁剪历史。

11. **业务迁移与批次交接。** 迁移 `freshrss_sync`、`index_pending`、`prune_old_documents` 及其手动触发、文档后台处理、HTTP 手动 Pipeline，继续复用现有业务用例与 `DocumentProcessingBatch`。文档待办与对应执行受理同事务保存；正常批次完成后仍有可处理待办时，本批完成与必要下一批受理在同一短事务提交，避免交接中断导致待办无人处理。没有可继续处理的待办时不续建，任务执行失败、取消或待核实也不自动续建；单篇文档异常仍按业务规则保留，不阻断其他正常资料。文档处理由业务待办驱动，不要求用户配置 cron，不保留常驻消费者占用 Worker 执行位置。

12. **业务完成依据。** 清理复用已有删除待办、原件引用和逐项确认。已完整完成的目标不会重新选中，未完成步骤按业务依据继续；远端成功但本地未记确认时允许安全地重复核对或请求，不能声称当前批次跨存储整体回滚。同步、索引及清理的互斥范围继续沿用，CLI 维护入口也参加原有协调。本次不改变清理资格、KnowledgeBase 范围、人工审核、已采用版本和正式检索可见性规则。

13. **时区与策略。** 全局调度时区默认 `Asia/Shanghai`，页面标明时区并预览未来执行时刻。具体计划、受理、开始、结束、重试和过期时间均按带时区 UTC 持久保存，保持 `DATABASE_TIMEZONE=UTC`。默认重试次数、间隔和历史天数由超级用户网页管理并保留修改记录；每次受理冻结策略，后续修改不追溯影响旧执行。任务配置仍由任务自己的持久模型承载，不复制进全局字典，也不把其他尚未完成的全局管理能力作为本次实施前提。

14. **历史与关联。** 普通已结束记录按结束时间及受理时的保留策略清理，默认三十天。未结束、待核实和恢复依据受保护；人工重试所关联的原失败记录在关联执行仍保留期间也受保护。普通详情过期后继续保留最小去重回执，包括主体、请求身份与摘要、原执行编号，不保留参数和结果正文；原请求重发返回原编号并说明详情过期，不能创建新执行。已清理参数快照的历史不提供重试能力，使用当前配置需重新提交。业务待办按业务规则保留，不能随任务历史清理。

15. **页面与权限。** “任务管理”分别提供周期配置和任务执行视图，接入一次性执行和按编号独立查询。展示任务类型、来源、状态、时区时间、尝试次数、等待或失败原因，以及适用的立即执行、取消和重试操作。常用周期选择与原始 cron 输入共用一个值和同一预览。离页不取消任务，恢复查询不依赖最近历史列表；超时保持原请求身份，不用新标识盲目重发。未知类型和未知结果保留可识别信息，部分失败不显示为全部成功。全局管理仍限超级用户，业务页面沿用所属业务权限。

16. **Worker 与 Redis。** API、单个 Beat 和 Worker 使用同一后端镜像、不同进程入口，首期一个业务队列，Worker 可增加进程或实例。生产采用 Linux prefork，子进程在 fork 后惰性建立独立持久事件循环及数据库依赖，按执行创建业务 Runtime；不继承 API 的异步池，不跨事件循环复用旧连接。Windows 原生可开发页面、API 和运行离线测试；生产同模式的多进程验收使用 Linux，可选远端 Linux、Docker 或 WSL，原生 solo Worker 的真实受理、补投、连续执行与正常关停已验证，Linux prefork 故障边界另行验收。消息完成后确认，预取量保持较小，Worker 丢失与可见性超时产生的重投必须经过数据库领取和恢复判断；不能靠无限增大超时解决长任务问题。Redis 是项目共用中间件，连接使用独立的 `REDIS_URL`，任务只保留 `TASK_QUEUE_*` 专属参数，并按队列名隔离消息与辅助键；缓存等其他用途各自管理键前缀。共享 Redis 使用 AOF、持久数据盘和实例级 `noeviction`，缓存按 TTL 过期，内存满时拒绝新增写入；连接、容量及告警放部署配置，凭据不进入前端构建、消息或执行快照。状态与结果只由 PostgreSQL 保存，不启用 Celery result backend。

17. **迁移与运行维护。** 迁移现有配置、执行身份、关联和必要状态，核验旧记录可用的参数与恢复依据，不删除业务待办，不用最新配置伪造旧执行快照。老板说明开发阶段任务均已停止，实际切换前仍需核对旧执行与占用。停用旧调度器和消费者后再切换新执行入口，禁止无保护混跑；同时更新安装、配置、OpenAPI、能力导航、执行流程及恢复说明。至少能观察 Beat 推进、投递失败、Worker 领取、业务等待与待核实。正常停机停止新领取并等待当前工作，强制中断按已定义的恢复规则处理。

## 测试决策

以“通过公开入口受理任务，再按执行编号查询状态和结果”为主要测试接缝，沿用已定稿 ADR 的验收范围。取消、重试、配置变更和权限通过同一组公开入口验收。cron 使用受控时间验证公开计算结果；真实队列与跨存储故障补充在现有集成边界上，避免为了测试给每个私有方法增加接口。

好测试验证用户可观察的状态、业务数据和失败后的去向。不会固定私有函数名、无语义的内部调用顺序、CSS 类名或完整页面文案。可用无外部副作用的测试任务贯通公共受理与执行，扩展性通过注册新类型后仍能完成周期及一次性执行验证。

已有先例可以直接复用其结构：

- `create_offline_app`、`TestAuthGate`、`test_trigger_returns_202_with_run_receipt` 和 `test_validate_cron_returns_utc_and_local`：HTTP 权限、受理回执与时间表达；调整旧回执固定为执行中的断言。
- `isolated_database`、`child`、`test_http_workers_conflict_and_cron_skip_preserve_receipt`：随机 PostgreSQL schema、独立子进程连接和并发受理；保留隔离设施，业务实际执行改由 Worker。
- `test_crashed_owner_requires_explicit_recovery`、`test_cleanup_waits_for_index_and_blocks_new_sync`、`test_terminal_save_failure_never_repeats_business_or_reports_confirmed_success`：资源互斥、旧占用保护和结果保存失败边界。
- `test_batch_keeps_review_failures_and_adoptions_distinct`、`test_batch_does_not_drain_unbounded_work_or_retry_failures`：文档批次上限、待审核与失败分开、失败候选不无限重领。
- 任务管理页面已有的重复点击、离页后按原回执查询、超时不盲目重发和未知类型可见测试，以及文档管理页面的在途轮询测试：扩展一次性执行、等待、取消、重试和配置删除后的查询。

旧测试锁定的“必须先停用才能编辑”“有活动执行不能删除配置”“按条数保留历史”“HTTP Pipeline 等待最终结果并返回 200”“常驻消费者自行轮询”需随新需求调整。保留原参数校验、权限、业务结果统计、逐项失败和资源收尾的保护。不能为了保持旧测试通过而恢复已经取消的旧行为，也不能仅删除旧断言而遗漏新行为验证。

验收项目如下；实施时在工程记录中登记对应证据：

| 编号 | 验收边界 | 必须观察到的结果 |
| --- | --- | --- |
| T01 | 公共任务接入 | 新注册的任务类型无需修改公共核心，即可由周期或一次性入口受理、执行和查询；一次性任务无需周期配置，业务状态仍由业务管理。 |
| T02 | HTTP 与权限 | 受理返回 202 和真实状态，执行按编号独立查询；未登录、非超级用户和业务范围越权得到原有拒绝；Pipeline 最终统计从执行详情读取，错误与参数保持脱敏。 |
| T03 | cron 与时间 | 历史五段式表达式的星期、月份、组合规则和分钟精度保持；北京时间 09:00 对应同日 UTC 01:00，00:30 对应前日 UTC 16:30；预览与实际计划一致，恢复不补历史周期。 |
| T04 | 请求去重 | 同请求重发返回原编号；配置在两次请求之间修改也不新建；相同标识换内容报冲突，不同操作的相同参数不合并；并发请求不突破唯一受理。 |
| T05 | 持久受理与投递 | 数据库提交失败不报告已受理；提交后发布前退出、发布失败、Redis 消息丢失均可补投原执行；重复或迟到消息不会并发启动同一业务尝试，不增加业务重试次数。 |
| T06 | 配置竞争与重叠 | 排队、等待资源或重试中的同配置新周期跳过，新的人工请求返回冲突；修改、停用、删除与受理按事务先后使用正确快照；旧版本回调不受理新执行，删除后已有执行继续。 |
| T07 | 自动与人工重试 | 默认初次加三次自动重试，间隔增加，耗尽后失败但下一周期独立执行；自动重试保留原参数和策略，人工重试用旧参数、新受理策略、新编号并可追溯原失败。 |
| T08 | 取消竞争 | 排队、尚未开始的资源等待及等待重试可取消；取消成功后旧消息不调用业务，Worker 已经开始业务则拒绝取消；取消与开始之间没有两边都成功的结果。 |
| T09 | 业务资源等待 | 多执行争用资源时原因可见，让出 Worker 执行位置且不消耗重试次数；释放后继续同一次执行；同步、索引、清理及 CLI 仍遵守原互斥范围。 |
| T10 | 崩溃与不确定结果 | 在领取前、开始后、远端成功后和终态保存时中断，分别进入补投、安全恢复或待核实；心跳过期不自动抢占；结果保存失败不能直接重做已经完成的业务。 |
| T11 | 文档批次持久交接 | 待办与受理同事务，本批完成与必要下一批受理同事务；任一交接处退出后仍有推进依据；无可处理待办时不空转续建，执行失败、取消或待核实不绕过限制续建。 |
| T12 | 现有业务不回退 | 文档处理不依赖用户 cron，单篇异常不阻断其他正常资料，待审核不算已采用；已采用正文与索引可见性、清理资格和知识库范围保持，批次完成标记保留。 |
| T13 | 跨存储部分成功 | PostgreSQL、Qdrant、原件存储各步骤失败或确认丢失后仅继续未完成工作，远端成功不被误称为回滚；普通历史清理及配置删除不丢失删除待办。 |
| T14 | 历史和策略 | 普通记录按结束时间及受理时策略过期；调整默认值不追溯旧执行；保护未结束、待核实和原失败关联；详情过期后旧请求仍返回原编号且不新建，已清理快照不能人工重试。 |
| T15 | 页面完整流程 | 两个视图覆盖周期与一次性执行，周期选项和原始 cron 一致，时区明确；离页后续查、删除后查询、等待原因、取消、重试和未知结果均正确，权限与原导航行为保持。 |
| T16 | 真实进程和 Redis | 使用真实 Redis、Beat 和多个 Worker 进程验证受理到结果、重投、连接恢复及关停；异步资源没有跨进程或跨循环复用，Redis 持久化重启及长任务超时重投遵守相同领取规则。 |
| T17 | 迁移与就绪 | 隔离数据迁移后配置、旧执行和恢复依据可解释；新旧调度与消费者不混跑，API 不直接执行本次迁移的后台工作；能区分调度、投递、领取和业务阻塞，实际切换结果单独记录。 |

离线测试默认不得访问真实依赖，沿用现有离线应用和连接阻断机制。前端 HTTP 替身只证明页面行为，Celery eager 模式也不能证明进程隔离、真实消息确认或恢复。

真实 PostgreSQL 及 Qdrant 验证优先复用随机 schema、Collection、Alias 和合成数据的隔离设施。新增 Redis／Beat／Worker 集成夹具同样隔离队列和测试进程，只清理本次创建的资源。共享开发 Redis 不可为了故障验证整体重启或清空；重启和消息丢失实验使用明确授权的隔离服务或隔离实例。

现有部分文档测试的 Qdrant 默认是内存实例，现有 PostgreSQL／Qdrant 清理组合测试的原件存储仍有替身；独立真实 S3 测试只证明原件接口本身。T13 必须分别记录哪些存储真实参与，发布所需的完整跨存储恢复另做真实组合验收，不能把已有测试名当作三个真实存储全部验证的证明。真实服务访问、数据库写入与故障操作在获得对应环境授权后执行。

## 超出范围

- 开发数据库修改及非用户数据处理已获老板授权，用户数据保留；真实服务切换及进程故障实验仍需落实对应环境与授权。
- 不同时接入 RabbitMQ 或 RocketMQ，不建设通用事件总线、多业务工作流引擎或动态插件系统。
- 不新增通用批次进度、批次续执行平台或独立人工恢复页面；不把现有整次资源占用改成按批次释放。
- 不提供正在进行的业务尝试的取消，不自动补执行错过的周期，不根据心跳过期强制释放旧写资源。
- 不启用第二套任务结果存储，不承诺跨存储操作恰好一次或统一回滚，不用 Redis 锁替代已有业务写入保护。
- 不改变文档解析与审核规则、索引规格、正式可见性和业务数据保留规则；不迁移所有 CLI 或 Agent 对话。
- 不把全局字典、FreshRSS 管理和模型连接管理等其他待实施能力并入本次重构，也不引入第二套任务配置来源。
- 不预建多队列或多 Beat 的高可用体系，不因本次页面改名增加重复管理入口。

## 补充说明

实施以本 spec 和 [ADR 0019](../adr/0019-scheduled-execution-and-write-coordination.md) 的目标方案为准。ADR 0014、0017 及 ADR 0019 末尾的旧规则用于理解迁移来源，不能覆盖本次已确认的行为；知识库业务仍遵循 [ADR 0023](../adr/0023-durable-intake-and-document-review.md)。配置归属保持业务专属配置与全局默认策略的区分，本次不等待其他管理能力完成。

源码与已有验证入口通过 [能力地图](../FEATURE_MAP.md)、[后端架构](../../backend/docs/architecture.md)、[现有任务执行流程](../flows/scheduled-job-execution.md) 和 [文档生命周期](../flows/file-document-lifecycle.md) 定位。上述文档仍描述当前实现；实施相应行为后同步更新，不能提前把待实施能力写成已上线。

### 实施顺序与进度

准备工作已完成，开发阶段按 P1 至 P8 推进。每项完成时登记实际改动、对应 T 编号及验证证据；阶段引用的 T 编号表示本阶段应完成的覆盖，完整端到端结果在 P7、P8 收口。代码完成但集成尚未验证时分开标注，不能提前勾选整项。

- [x] 完成需求访谈，记录周期、一次性执行、配置快照、重试、取消、UTC 和历史规则。
- [x] 完成架构收敛与设计消融，确认批次持久交接和业务恢复边界。
- [x] 核对现有 HTTP、页面、数据库并发及文档批次测试接缝，记录替身验证的范围。
- [x] 生成施工规格，完成模板、编号、本地链接校验和独立范围复核。
- [x] P1 公共契约与持久模型：类型注册、执行状态、快照、独立执行查询、原请求回执和必要唯一约束已实现。公共契约离线验证、真实旧结构迁移／约束／回滚验证及开发库升级通过；保留历史与恢复依据，覆盖 T01、T02、T04、T14、T17 的本阶段部分。真实消息及页面联调继续在后续阶段收口。
- [ ] P2 统一受理与投递：接入持久受理、原请求去重、发布和补投、Worker 原子领取与条件收尾。完成条件：提交后故障和重复投递仍只推进原执行，API 不直接调用任务处理函数；验证 T04、T05、T10。
- [ ] P3 Beat 与 Worker 进程：接入动态 PostgreSQL 调度适配、统一 cron 计算、UTC 转换、Redis 配置及子进程异步依赖生命周期。完成条件：唯一调度来源、真实队列到 Worker 通路成立，漏跑不补且已受理保留；验证 T03、T05、T16。
- [ ] P4 执行操作与管理策略：实现配置并发生效、自动重试、人工重试、取消、资源等待、待核实和策略快照，接入历史清理及保护。完成条件：操作竞争有唯一结果，重试、恢复和保留策略不相互绕过；验证 T06 至 T10、T14。
- [ ] P5 现有业务迁移：将三类定时任务、文档后台批次及 HTTP Pipeline 接到 Worker，完成待办与受理、批次完成与续建的持久交接，保留 CLI 共享资源协调。完成条件：全部范围内入口经 Worker 推进，原业务结果与失败边界保持；验证 T01、T02、T09、T11 至 T13。
- [x] P6 任务管理界面：两个视图、常用周期及原始 cron、独立详情、等待、取消、重试和策略管理已接入；类型由 OpenAPI 生成，未知及部分失败结果有行为回归。真实登录／API／Redis／Beat／solo Worker 页面联调通过创建、回执丢失确认、离页续查、取消、失败重试、策略留痕、启用时编辑及删除后的独立查询，桌面与 390px 显示通过。覆盖 T02、T03、T06 至 T09、T14、T15 的页面边界，进程故障归 P7/P8。
- [ ] P7 迁移与部署配套：更新启动配置、就绪与故障说明、契约文档及能力导航，移除旧调度和消费者入口；准备并按授权执行隔离迁移、真实队列和跨存储验收。完成条件：T13、T16、T17 有明确环境及结果记录，未执行的真实切换单独保留待办。
- [ ] P8 必要回归与消融：按最终影响范围完成离线回归、前端检查与构建、已授权真实环境验收，复核新增分支、抽象和测试断言的独立保护价值。完成条件：T01 至 T17 均有通过或明确待处理记录；仍有必须通过的验收项时不标记重构完成。

### 验收证据与缺口（2026-09-13）

下表的“离线通过”只指本地受控依赖的行为验证。真实 PostgreSQL 的迁移、交接与并发三组 **13 passed**，补充配置竞争 **6 passed**，真实 PostgreSQL／Qdrant／S3 故障恢复 **5 passed**，文档业务 PostgreSQL／Qdrant 回归 **21 passed**，遗留纯计算的一次性交接 **1 passed**；加上 Windows 原生 HTTP／Beat／solo Worker **1 passed**，共 47 项实际通过。另有真实页面联调及数据库回执核对，P1、P6 已完成。Linux prefork 与运行服务切换仍待 CI 实际执行，不能用 solo 代替多进程故障证据。

| 编号 | 已有证据 | 尚待实际验证 |
| --- | --- | --- |
| T01 | [公共执行测试](../../backend/tests/test_task_execution.py)的 `test_new_type_executes_from_direct_and_scheduled_intake` 离线通过，覆盖注册新类型后的周期与一次性受理、执行和查询。 | 真实队列与已有业务装配的联合运行。 |
| T02 | [公共 HTTP](../../backend/tests/test_task_api.py)与 [Pipeline HTTP](../../backend/tests/test_pipeline_api.py)离线通过，覆盖 202、按编号查询、超级用户校验、原参数限制、统计及错误脱敏；OpenAPI 已重新生成。[本地真实联调](../../backend/tests/test_task_local_integration.py)与浏览器验证完成真实登录、HTTP → Worker → 执行详情。 | 本项已通过，生产多进程故障另归 T16。 |
| T03 | [cron 工具](../../backend/tests/test_scheduled_task_registry.py)的日历、星期及跨日用例与 [Beat 规则](../../backend/tests/test_scheduler_runner.py)离线通过，覆盖预览/计划一致、历史组合及不补漏。 | 对照真实 Beat 的配置变更与未来执行时间。 |
| T04 | [请求身份测试](../../backend/tests/test_task_execution.py)离线通过，配置变更/删除及详情过期后仍返回原编号，内容冲突被拒绝；[PostgreSQL 并发受理](../../backend/tests/test_scheduler_postgres_integration.py)及[迁移后约束](../../backend/tests/test_task_migration_postgres_integration.py)实际通过，同请求同编号、不同请求竞争一个名额。 | 本项数据库及请求契约证据已通过；真实消息通路归 T05、T16。 |
| T05 | [提交失败测试](../../backend/tests/test_scheduled_job_store.py)及 [公共执行测试](../../backend/tests/test_task_execution.py)离线通过，失败提交不发布、投递失败/消息丢失补投、重复消息不再执行业务；本地真实 Redis 消息丢失、Beat 补投与取消旧消息处理通过。 | Linux Redis 发布故障、AOF 重启及并行重复投递。 |
| T06 | [Beat/配置测试](../../backend/tests/test_scheduler_runner.py)离线通过，覆盖全部未结束状态的重叠、旧版本回调、启停编辑删除及原快照保留；[PostgreSQL 配置竞争](../../backend/tests/test_scheduler_postgres_integration.py)实际通过人工受理使用新快照，以及改／停／删分别先于或后于周期受理的六个场景；旧回调失效，已受理执行仍按原快照完成。 | 动态 Beat 真实进程验证。 |
| T07 | [重试额度与策略测试](../../backend/tests/test_task_execution.py)离线通过，覆盖初次加三次、递增间隔、人工重试关联、新策略及下一正常周期独立执行；真实页面／Worker 验证数据库错误失败后人工重试成功，原参数、原失败关联与新策略保持正确。 | 真实 Worker/Beat 的自动重试到期投递与多次尝试。 |
| T08 | [取消与准备清理](../../backend/tests/test_task_execution.py)离线通过，开始后拒绝取消，旧消息/旧结果不能覆盖取消或恢复结论；[PostgreSQL 开始/取消竞争](../../backend/tests/test_scheduler_postgres_integration.py)实际通过，开始与取消只有一方成功；真实页面取消后，solo Worker 消费旧消息仍保持取消且尝试为零。 | 本项已通过，多进程崩溃边界另归 T16。 |
| T09 | [资源等待测试](../../backend/tests/test_task_execution.py)离线通过，等待原因可见且不消耗尝试；[跨进程写资源互斥](../../backend/tests/test_scheduler_postgres_integration.py)实际通过清理等待索引、后续同步等待清理及明确恢复前不抢占。 | 资源释放后真实 Celery Worker 继续执行。 |
| T10 | [收尾与恢复测试](../../backend/tests/test_scheduler_safety.py)、[公共执行测试](../../backend/tests/test_task_execution.py)离线通过，完成结果保存重试不重做业务、旧领取不覆盖恢复结论；[真实三存储](../../backend/tests/test_task_cross_storage_integration.py)已通过两个远端删除回执丢失场景，保留待核实与写占用，核实后才继续。 | 真实 Worker 强制中断及完成证据恢复。 |
| T11 | [批次交接测试](../../backend/tests/test_scheduler_safety.py)离线通过，正常完成才续建，无待办不续建；[PostgreSQL 交接](../../backend/tests/test_task_handoff_postgres_integration.py)两项事务中断及一项遗留计算交接均通过。未提交事务退出整体回滚，重新保存仅有必要续批；旧库仅剩解析／预览领取也能受理首批，草稿与未决写入状态保留，旧计算结果失效。 | 真实消息进程归 T16；实际开发服务切换时的一次性交接归 T17。 |
| T12 | [文档批次](../../backend/tests/test_news_pipeline_execution.py)离线回归通过；真实 PostgreSQL／Qdrant 的接收、处理、审核、采用、删除、重建与文件 HTTP 回归 **21 passed**，覆盖旧版保留、迟到结果、失败恢复、正式检索与全文可见性。原件和 Embedding 为替身，解析与 tokenizer 为本地真实实现。 | 原业务经真实 Celery Worker 推进的联合运行；本组不能作为真实原件或模型验收，真实三存储删除恢复见 T13。 |
| T13 | [三存储恢复](../../backend/tests/test_task_cross_storage_integration.py) **5 passed**，真实 PostgreSQL／Qdrant／S3 共同验证远端删除后本地确认失败、数据库收尾失败及两个删除回执丢失场景；五份报告均确认远端测试资源清理，schema 随夹具退出清理。 | 本项组合故障验收已通过；Celery 消息进程和实际部署切换分别归 T16、T17。 |
| T14 | [策略/回执测试](../../backend/tests/test_task_execution.py)与 [失败关联保护](../../backend/tests/test_scheduler_safety.py)离线通过；[真实迁移](../../backend/tests/test_task_migration_postgres_integration.py)验证历史裁剪后回执返回原编号、缺失快照禁止重试、删除配置仍保留占用与删除待办；[三存储恢复](../../backend/tests/test_task_cross_storage_integration.py)验证清理历史不丢恢复依据。 | 真实队列中的策略变更与重试归 T07、T16；原失败关联保护当前只有离线证据。 |
| T15 | [任务管理页面](../../frontend/src/pages/ScheduledJobsPage.spec.ts)、[HTTP 适配](../../frontend/src/api/tasks.spec.ts)与 Feature 测试通过；真实登录、API、Redis、Beat 和 solo Worker 的页面联调通过创建、回执丢失确认、离页续查、取消、失败重试、策略留痕、启用时编辑及删除后按编号查询；1440px／390px 无横向溢出。 | 本项已通过；原件、模型等业务上游未在页面联调中调用。 |
| T16 | [队列夹具](../../backend/tests/test_task_queue_integration.py)七种场景已准备，含多子进程持久循环、AOF/重连、重复/可见性重投、关停与动态 Beat，默认跳过。 | Linux 上真实 Redis、Beat 与 prefork Worker 的完整运行及进程日志。 |
| T17 | [隔离迁移](../../backend/tests/test_task_migration_postgres_integration.py) **3 passed**，覆盖旧结构升级、约束与前提不满足时整体回滚；开发库已授权升级至 `b6e2f9047a31`，用户数据摘要及原有配置／执行身份一致。迁移 SQL、两份 Compose、发布脚本静态检查与本地就绪逻辑测试通过。 | 实际 Linux 进程就绪与诊断、旧入口停用、一次性业务待办交接和服务切换；未控制远端旧服务，不能据本机无进程推断远端已停用。 |

当前 Windows 环境没有本地 Docker/WSL，已通过原生 HTTP／Beat／solo Worker 和真实页面联调。Redis 当前密码为空，TCP 与 PING 通过。Linux prefork 验收已接入 GitHub Actions 部署前步骤，待推送后实际执行。隔离命令和资源范围见 [后端测试说明](../../backend/README.md#测试)，切换顺序见 [部署手册](../container_deployment.md)。老板已授权开发数据库非用户数据处理、本地测试，以及验证后分批提交、推送与跟踪部署；用户数据保留，共享 Redis 不整体重启或清空。

### 工程记录与完成条件

2026-09-13（首次 CI 结果与修复）：第三批配置与文档提交为 `1e5df62`，三批均已推送到 main。推送后立即检查 Actions，并按提交 SHA 确认运行 [34734349036](https://github.com/zhengkanghua/agent-lab/actions/runs/34734349036)。后端 **748 passed、63 skipped、1 warning**（47.09 秒），Linux 队列／迁移／并发／交接 **27 passed**（292.26 秒），前端 **809 passed**（70 文件），lint 与构建通过，后端镜像成功推送。候选编排上传因项目目录不可写而失败，后端部署步骤未执行，服务尚未切换。工作流补齐部署手册已有的目录归属要求，只调整该项目目录本身。

2026-09-13（Beat 退出故障与消融）：下载首次 CI 的 `task-processes` artifact 后，核实七份 Beat 退出日志均出现 `Runner is closed`／未等待协程。Celery 默认信号处理器立即关闭调度器，既会重入当前异步 tick，也会与主循环 finally 重复关闭。改为信号只请求停止、当前 tick 返回后统一收尾，并让调度器关闭可重复调用；Linux 队列用例补查 Beat 零退出码及未就绪状态。定向验证 **21 passed、7 skipped**（20.87 秒，Linux 用例在 Windows 跳过）。消融仅在独立测试进程断开新信号处理器，SIGTERM／SIGINT 两项都因 tick 未完成而失败，并出现协程未等待告警，证明此保护有实际价值；生产源码未改动。修复后的真实 Linux 关停证据待下一次 CI 确认。

2026-09-13（部署修复本地检查）：工作流 YAML 解析与全部 **11 段 Shell 语法检查通过**，包含新增的目录权限准备；四项其他工作改动的 SHA-256 与本轮基线一致。`git diff --check` 通过。该验证只证明本地配置及语法，目录实际写入和服务就绪须由下一次 Actions 验证。

2026-09-13（分批提交）：后端公共任务与业务交接提交为 `9975f1e`，前端任务管理提交为 `89c7e99`；Redis／部署／CI／文档作为第三批提交。最近的修改只涉及本地夹具、Linux 热停止验收退出码及验证记录；本地真实联调通过，Linux 夹具编译与默认门控 **7 skipped**，尚不代表 Linux 实测。最新三份文档 64 个链接与差异空白检查通过，先前已通过的全量回归不重复执行。浏览器／API／Beat／Worker 均已关闭，测试键及 schema 清理确认；其他工作的四项文件内容已建立摘要并保持不变。远端 main 为本地已有提交的祖先，推送将保留现有提交历史；准备继续跟踪 Actions 的 Linux 门禁和部署结果。

2026-09-13（真实页面联调通过）：Playwright 连接隔离 PostgreSQL、共享 Redis、真实 API／登录 Cookie／Beat／solo Worker。创建并预览定时任务后，真实受理完成再丢弃 HTTP 回执，离页返回后“确认受理”仍得到原编号；取消执行的尝试次数为零。临时改名本随机 schema 的 Document 表触发真实 SQL 故障，任务记录失败；恢复表后人工重试新建执行且成功，保留原参数和失败关联，采用新默认策略。启用时编辑成功，删除配置后三个执行仍可查询。接口最终核对恰好三个执行、原请求不重复、参数快照未被编辑追溯修改、策略快照与来源身份正确。1440px／390px 列表、详情与策略窗口验证通过；当前页面无控制台错误，早先未登录 401 和刻意丢回执的网络错误符合场景。截图为 `output/playwright/shared-tasks-real-*.png`，核对报告在 `backend/.pytest_cache/task-browser-live-1/browser-audit.json`；测试 schema／Redis 键已清理、浏览器和本地进程已关闭。临时浏览器驱动与故障注入留在忽略目录，不进入生产实现。

2026-09-13（Windows 真实联调通过）：修正夹具就绪判断后，`tests/test_task_local_integration.py` **1 passed**（113.17 秒）。真实 PostgreSQL、共用 Redis、HTTP 认证、Beat 和 solo Worker 共同验证登录／Cookie、同请求去重、取消、丢消息后补投、取消旧消息不能开始业务、同一 Worker 连续执行和正常关停关闭 Runtime；业务使用生产清理处理函数对空 KnowledgeBase 预演。报告 `backend/.pytest_cache/task-local-live-2/test_http_beat_and_solo_worker0/report.json` 的 `redis_keys_cleaned`、`schema_cleaned` 均为 true。该结果不覆盖 Linux prefork 或真实业务上游，页面与 CI 继续推进。

2026-09-13（首次真实本地联调）：真实登录／Cookie、202 受理、同请求去重、取消及丢消息后的 Beat 补投均进入真实进程，生产清理预演已成功；夹具在 Beat 就绪等待超时，整体 **1 failed**（185.65 秒）。诊断确认 Windows 虚拟环境启动器 PID 与实际 Python PID 不同，修正为核对本次独立就绪文件、启动器存活及文件内实际进程存活，未放宽生产逻辑。报告确认随机 Redis 键和 schema 均已清理。独立核验另发现 Linux 热停止断言可能把兜底强杀当作成功，已让停止助手返回退出码并在热停止用例要求正常退出；两项修正待真实重跑／CI 验证。

2026-09-13（Redis 连通恢复）：按老板要求复测，TCP 连接与 Redis PING 分别在 0.03 秒、0.02 秒通过。当前 `REDIS_PASSWORD` 为空，实际验证的是无密码连接；非空密码的参数装配已有离线验证，不冒充真实密码认证。已开始真实 PostgreSQL／共享 Redis／HTTP／Beat／Windows solo 联调，使用随机 schema 和队列前缀，不重启或清空共享服务。日志写入 `backend/.pytest_cache/shared-task-local-live-1.log`，结果按实际运行继续补记。

2026-09-13（Redis 配置归属与本地联调，进行中）：按老板要求将 Redis 作为项目共用中间件，新增独立 `RedisSettings` 与 `REDIS_URL`，移除任务队列配置中的连接字段。任务执行参数保留 `TASK_QUEUE_*`，Celery 按队列名给消息与未确认消息等辅助键增加统一前缀；Compose 服务、数据卷、网络与内存变量同步通用化，部署脚本与文档引用一并调整。配置装配及实际 Redis 命令前缀的离线检查通过，消融确认去掉前缀后不同队列共用 `unacked` 等辅助键；公共执行、Beat 与生命周期定向回归 **27 passed、7 skipped**（8.71 秒），真实 Linux 队列仍按门控跳过。

老板随后已配置共用 Redis，要求新增独立 `REDIS_PASSWORD`（留空不使用密码），并授权本地测试、验证后分批 commit、push 与跟踪 GitHub Actions。密码通过客户端参数传入，不拼接 URL；隔离 Linux 夹具显式清空密码，避免继承开发配置。补入 solo Worker 主进程关闭时的资源收尾，准备使用随机数据库 schema 和 Redis 键前缀进行 Windows 原生联调；共享 Redis 不重启或清空。其他工作仍保留，尚未提交或推送。

密码装配检查覆盖空值及包含特殊字符的值，Worker 关闭信号的定向验证 **11 passed**；提交前后端全量回归 **748 passed、62 skipped、1 warning**（72.20 秒），唯一警告仍为内存 Qdrant 不支持 payload index。已配置 Redis 的首次 PING 与独立 TCP 检查均超时；域名解析成功，6379 端口在 7 秒内未完成建连，尚未进入认证。已向老板核对网络可达方式，未改动共享 Redis。新增本地 solo/HTTP/Beat 联调夹具与真实认证、空知识库预演路径，尚未实际运行；Linux prefork/迁移验收加入 GitHub Actions 部署前步骤，尚未推送执行。

本轮本地收口：新联调用例默认门控与生命周期回归 **11 passed、1 skipped**（7.41 秒）。复核把关闭断言放在测试兜底清理之前；正常实现 **11 passed**（5.97 秒），独立进程临时断开 solo 关闭信号后，该用例按预期失败，`ablation_without_solo_shutdown_fails=true`，生产源码未因消融改变。两份 Compose 官方解析、服务／网络／卷引用核验、部署工作流的 10 段 `bash -n`、11 份文档的 101 个本地链接、Python 编译及差异空白检查均通过。新本地联调、真实页面和 Linux CI 仍待执行，前端代码未再次修改，沿用已通过的前端回归与构建。当前没有运行中的测试或本地服务；按老板“测试没问题再提交”的要求，Redis 网络未恢复前保留未提交改动，尚未 commit、push 或部署，spec 继续保留。

2026-09-12（本轮收尾）：授权范围内的真实数据库与存储验收共 46 项通过，相关日志已保存；没有运行中的验收进程。本轮代码编译、修改文件差异空白检查与四份文档的 69 个本地链接检查通过。未改动前端运行代码，不重复执行已通过的前端全量、构建及浏览器替身验证；未重复全量后端回归，只对新增配置竞争与交接修复做定向验证。P2 至 P8 剩余的真实 Redis／Beat／prefork Worker、对应 HTTP／页面联调和运行服务切换需要 Linux 环境，本机 Docker／WSL 缺失仍未解除。其他工作的改动保留，未执行 Git 写操作，spec 继续保留。

2026-09-12（遗留纯计算交接修复与消融）：独立只读核验发现，旧库如果只剩 `processing` 解析／预览领取，一次性 bootstrap 原本只查可处理待办，不能创建首批；超时回收又需要先有批次启动。修复在装配边界复用 `PostgresProcessingRepository.requeue_computations`，在已确认旧消费者停止的切换阶段回收纯计算，并沿用事务受理；不增加日常 Beat 扫描或第二套状态更新 SQL。索引准备、发布及接收未决写入保持原有核实边界。新增真库用例 **1 passed、2 deselected**（66.80 秒），验证仅遗留纯计算时的首批、同次交接去重、人工草稿保留及迟到计算结果失效，日志为 `backend/.pytest_cache/shared-task-legacy-bootstrap-attempt-2.log`。首轮因合成记录缺少领取所需的原件元数据而失败（50.96 秒），补齐夹具后通过，未绕过生产校验。

消融在独立测试进程中临时省略纯计算回收，原测试准确失败在 `run_id is not None`，即没有首批受理，`ablation_expected_failure=true`（51.21 秒）；原源码没有被修改，正常修复保留。日志为 `backend/.pytest_cache/shared-task-legacy-bootstrap-ablation.log`；该嵌入式 pytest 调用另有一条已导入 anyio 的断言重写提示，不属于业务失败。开发库只读复核为 12 条 `adopted` 处理记录，100 条历史仍全部成功，未发现遗留 `processing`；实际开发库尚未执行业务交接或启动新 Worker。

2026-09-12（T06 与文档业务回归完成）：新增改参数与 cron、停用、删除分别先于或后于周期受理的六个 PostgreSQL 场景全部通过，**6 passed、8 deselected**（398.95 秒）。通过 `pg_blocking_pids` 确认另一进程确实等待配置行锁，再经生产配置和受理用例提交；旧回调不新建，已受理执行保留原快照并继续完成。日志为 `backend/.pytest_cache/shared-task-configuration-races.log`，未重跑已通过的十三项。同轮文档 PostgreSQL／远程 Qdrant 回归 **21 passed**（2627.19 秒），日志为 `backend/.pytest_cache/shared-task-business-postgres-tests.log`；原件与 Embedding 为替身，不将其算作真实 S3 或模型证据。代码编译、修改文件的差异空白检查以及 spec／ADR 的 43 个本地链接检查通过。

2026-09-12（开发库升级与三存储验收完成）：按老板授权，开发库已从 `a91b3c7d5e20` 升至 `b6e2f9047a31`。迁移事务内锁定任务相关表，复核两条周期配置均停用、100 条历史均成功且无写占用；用户数据摘要和任务身份一致才提交，提交后新连接再次核对版本及摘要。3 个账号、37 条登录凭据、2 条 Agent 会话归属、四张 checkpoint 表及 12 条审核记录均保留，未清空现有业务数据。报告为 `backend/.pytest_cache/task-development-migration.json`，`committed`、`user_data_preserved`、`task_identities_preserved` 均为 true。没有切换 API／旧 scheduler／Worker 等实际服务。

同轮真实三存储验收 **5 passed**（965.76 秒），日志为 `backend/.pytest_cache/shared-task-cross-storage-tests.log`。覆盖 Qdrant/S3 删除后数据库确认未保存、PostgreSQL 收尾失败，以及 Qdrant/S3 实际删除后应用收到异常；后两项保留待核实及写占用、拒绝普通重试，核对远端事实并明确确认后再继续。五份 `task-cross-storage-*.json` 报告均为 `completed` 且 `remote_resources_cleaned=true`，对象、Alias、Collection 分别核对清理；随机 schema 随夹具退出清理。P1 合并公共契约离线证据、真实迁移／唯一约束及开发库用户数据复核后标记完成。文档 PostgreSQL／远程 Qdrant 回归已通过前 12/21 项，原件与 Embedding 使用替身，仍在运行；周期配置竞争的剩余事务顺序准备补强。

以下保留各阶段当时的工程记录；“尚未迁移／未授权”等描述以本文开头及上述最新记录为当前状态。

真实 PostgreSQL 验收通过：修复迁移 JSON 绑定问题后，旧结构迁移、批次交接和并发任务三组共 **13 passed**（862.85 秒）。随机 schema 覆盖迁移保留与回滚、持久交接退出、受理/配置竞争、取消竞争及写资源互斥；日志为 `backend/.pytest_cache/shared-task-postgres-attempt-2.log`。修复后的离线迁移 SQL 也已重新生成。三存储组合验收已开始，Qdrant 删除后数据库确认丢失场景已通过，其余仍在运行。准备按老板授权升级开发库并在同事务核对用户数据摘要与任务记录身份。

首次真实 PostgreSQL 验证：开发库当前为 `a91b3c7d5e20`，两条周期配置均停用、100 条任务历史均成功、无写占用；已建立账号、登录凭据、Agent 会话与审核记录的计数/摘要基线。隔离升级第一项发现新迁移内的 JSON `:true` 被 SQLAlchemy 识别为绑定参数，实际升级失败；已改为 `jsonb_build_object`，准备重跑。故障发生于随机 schema，应用数据库尚未升级，用户数据未修改。

数据库授权后继续实施：老板明确“数据库可以改，还在开发阶段……除了用户数据，其他数据都可以处理掉”。据此执行开发库核对、隔离 schema 验收及必要迁移，不再重复请求数据库写入许可；账号、登录凭据、Agent 会话及相关用户记录保持不变，并核对迁移前后摘要。发现新交接夹具缺少 `conftest.py` 的真实连接开关登记，已补齐后再运行。当前仍未执行应用数据库迁移或服务切换，后续按实测更新。

环境与授权阻塞确认：连续三轮目标推进均未取得真实隔离验收的环境与操作授权。本机检查确认没有 Docker 命令，`wsl.exe --list --quiet` 返回退出码 1，并明确提示尚未安装 Windows Linux 子系统。当前没有待观察的验收进程；本地实施、回归及夹具准备均已收尾，重复运行相同离线检查不能补齐真实证据。目标标记为阻塞，保留全部代码和本 spec；取得 Linux/Docker 环境及限定测试资源的写入、清理、进程控制授权后，从表内真实验收缺口继续，未经验证不标记重构完成。

2026-09-12（本地收尾）：T01 至 T17 的证据与缺口已逐项登记。最新 spec/后端 README 的 29 个本地链接目标检查通过，差异空白检查通过。消融复核保留事务交接、领取代次和写入不确定保护，删除测试资源清理中仅依赖“收到创建回执”的冗余状态，改为核对真实存在情况；未增加通用故障框架或生产恢复分支。本次页面验证的浏览器会话和两个本地模拟服务已关闭，确认对应端口不再监听。其他工作的未提交改动保留，未执行 Git 写操作、真实服务操作或应用数据库迁移；spec 保留，等待真实隔离验收环境与授权。

2026-09-12（T03/T07/T11 证据补强）：补入历史 cron 的星期编号、星期名称、月份、日期/星期组合及北京时间跨日预览，按固定日期核对预览与计划；扩展重试用例验证额度耗尽不影响下一正常周期独立受理。新增随机 PostgreSQL schema 的交接中断夹具：子进程在待办/受理写入后、以及本批终态/下一批受理写入后退出，核对整体回滚和重新保存后的唯一交接；只构造合成业务待办，不把它当作原件/模型或真实 Worker 恢复验证。已加入隔离验收镜像和运行命令；定向运行公共执行、注册/cron 及新交接夹具为 **28 passed、2 skipped**（23.06 秒），真实 PostgreSQL 仍未连接。

2026-09-12（T10/T13 夹具收尾）：三存储夹具新增 Qdrant/S3 实际删除成功但应用收到异常的两种场景，复用生产写入不确定标记，检查待核实、保留写占用、历史保护、拒绝普通重试及明确核实后继续。仅对已结束的随机测试执行模拟心跳静默期，不放宽生产恢复条件。另修复创建回执丢失时测试资源可能漏清理的问题：按本次精确 Collection/Alias 名查询实际存在情况，删除后再次确认才记录成功。定向检查 **5 skipped**（27.78 秒），五种场景收集正常、门控生效，均未实际访问三个存储。

2026-09-12（P8 本地回归与配置核验）：最终后端全量离线回归 **742 passed、51 skipped、1 warning**（69.69 秒，新增两种三存储场景前）；唯一警告为本地 Qdrant 的 payload index 限制。最新迁移离线 SQL 生成通过；重新导出 OpenAPI 并生成前端类型后，前端 `typecheck`、全量 `lint`、`format:check` 均通过。两份 Compose 用官方 v2.39.4 解析通过，未连接 Docker daemon；发布工作流 YAML 解析及其中 9 段 shell 脚本 `bash -n` 通过，未执行部署。11 份文档的 67 个本地链接有效，`git -c core.safecrlf=false diff --check` 通过。前端全量 **809 passed**、生产构建及消融后的定向验证沿用同一代码已有结果，不重复运行。T13/T16/T17 真实证据仍待授权环境提供。

2026-09-12（P7 文档与三存储夹具）：根、前后端 README、架构、能力导航、任务／文档／新闻流程、部署手册和 ADR 0019 状态已同步到新实现，保留旧决策历史并明确环境未切换。新增三存储组合夹具，通过公共受理与 Worker 业务接缝运行真实 PostgreSQL/Qdrant/S3 删除，分别注入两类远端已确认删除但数据库确认未保存，以及 PostgreSQL 收尾失败，并检查完整成果不重做、历史清理及配置删除不丢待办。队列／迁移 **10 项收集通过**；加上三存储后默认执行 **13 skipped**，门控生效、未连接真实服务。部署 YAML/脚本、最新迁移 SQL、OpenAPI 及最终必要回归待继续核验。

2026-09-12（P7 配套与夹具核验）：已改 Compose 为专用持久 Redis、API、单个 Beat 与可扩容 prefork Worker；发布脚本准备候选 Compose、先停旧入口再迁移并分别核验三个进程，尚未执行部署。新增 Linux 隔离验收镜像及真实 Redis／PostgreSQL 队列夹具，覆盖重投、AOF、Worker 重连和关停、动态配置与策略重试；独立静态核验发现 visibility 扫描间隔远大于 2 秒超时、重复消息检查过早和日志未持久保存，已调整为等待真实重投和处理证据、保存容器测试日志。补写从 `a91b3c7d5e20` 升级的隔离迁移测试；亲读旧迁移发现新脚本外键名称错误，已改为历史实际名称。以上新夹具尚待离线收集检查，真实 T13／T16／T17 未执行；当前 Windows 未发现 Docker 或 WSL，不据此宣称真实 Linux prefork 验收通过。

2026-09-12（P6 验证与消融补记）：lint 修复后的页面与 API 定向测试 **26 passed**，相关 Feature、页面与 API 的 ESLint **通过**。Playwright 使用本地内存接口完成桌面、390px 移动端列表／详情及策略对话框核验，未发现横向溢出或新增控制台错误；截图在 `output/playwright/shared-tasks-*.png`，不作为真实后端证据。删除无调用方的旧 Pipeline 写 Runtime getter、同步响应 DTO、错误响应包装和三种旧编辑／执行异常，保留业务仍依赖的安全错误分类；后端相关 HTTP／错误／生命周期测试 **48 passed**。P6 整项仍待真实接口联调。

2026-09-12（P6 首轮实现）：前端已生成新 OpenAPI 类型并接入两个视图、按编号详情、取消、人工重试、策略与修改记录、手动 Pipeline、常用 cron；原请求标识与参数在提交前按账号保存，超时后显式确认同一请求。配置启用/执行期间可编辑和删除，未知结果与部分失败独立展示。定向测试最初 **51 passed**，加入公共 HTTP 契约后全量为 **809 passed**（70 文件）；生产构建（含类型检查）与格式检查通过。lint 发现 5 处子组件修改 props 和 6 处页面深度导入，已改为状态动作与 Feature 公共导出，等待修复后定向回归。此前检查会话中断且结果不可查询，已重跑并把输出保存在本地缓存日志。浏览器使用本地模拟接口核验中，真实接口/Worker 尚未联调。

2026-09-12（后端离线回归收口、P6 开始）：`uv run --no-sync pytest -q --tb=short` 为 **742 passed、38 skipped**，耗时 75.46 秒；跳过项为门控真实依赖测试，另有本地 Qdrant 不支持 payload index 的预期警告。业务受理、领取/取消、恢复、批次交接与 HTTP 的最新修复已包含在该回归中；不据此证明 PostgreSQL 锁、Redis 或跨存储恢复。通过不启动 lifespan 的应用导出 OpenAPI，并用 `openapi-typescript` 生成前端契约；开始任务管理两个视图、独立执行详情和请求身份持久化。P1 至 P8 整项继续保持未完成。

2026-09-12（业务与 HTTP 验证）：Pipeline 已恢复同步阶段仅占用 `sync`、同步结束立即释放，处理批次沿用原采用/回收边界。公共执行、Pipeline、任务注册及有界文档批次一组 **47 passed**；调度、收尾、维护、存储一组最初 **26 passed、3 failed**，失败均为离线固定时钟早于业务受理时间，已修正测试时钟并通过对应批次测试；配置 HTTP 与批次交接一组 **34 passed、1 failed** 暴露组件不可用未映射 503，已补齐。随后公共 HTTP、配置 HTTP、错误契约与门控 PostgreSQL 测试为 **45 passed、12 skipped**。真实 PostgreSQL 测试已迁到统一受理/Worker 接缝，保留随机 schema、独立进程、旧占用及保留期用例；未授权，未连接真实服务。开始全量离线回归，P6 前端尚未实施。

2026-09-12（领取与恢复修复）：准备占用绑定本次领取 token，取消和失联恢复在执行行锁内清理未开始的准备，迟到的准备不能重新创建占用；已开始的业务仍保留旧写保护。维护入口接入 `needs_attention`，安全恢复额度耗尽记失败，旧结果不能覆盖恢复结论。新增内存 SQLite 离线接缝，`uv run --no-sync pytest -q --tb=short tests/test_task_execution.py` 为 **9 passed**，覆盖 T01、T04、T05、T07、T08、T10、T14 的状态路径；SQLite 不证明 PostgreSQL 并发锁、真实 Redis 或迁移。独立核验发现并补入知识库重新启用、超时计算重排队、重建失败/发布终止的事务受理，并修正完成与重试的反向锁顺序；这些最新补入点尚待回归。Pipeline 的新增外层资源占用扩大了原范围，正在修正。阶段复选框继续保留未完成。

2026-09-11：生成本 spec，完成模板、用户故事与验收编号、本地链接校验及独立范围复核。已完成只读源码与测试先例核对，业务实现、业务测试、数据库迁移、真实 Redis／Worker 和跨存储故障验收均未开始。

2026-09-12（实施基线）：已核对工作区与根协作约定、术语表、ADR 0019/0023、模块规范及测试边界。当前 HEAD 为 `36195c2`，保留已有 `AGENTS.md` 修改及未跟踪的 `.tmp-tw.css`、`.workbuddy-ai/`、`docs/adr/0024-global-business-configuration-and-admin-management.md`。P1 开始；P1 至 P8 尚未完成，真实数据库及服务操作未执行。

2026-09-12（P1 至 P5 开发中）：新增 `tasks/` 公共契约、受理/投递/领取与条件收尾，扩展执行快照、原请求回执、策略留痕和唯一约束；迁移脚本为 `b6e2f9047a31_shared_task_execution.py`。新增 Celery `5.6.3`、动态 Beat 和每子进程持久事件循环入口；API 改为受理与查询，移除旧 APScheduler 执行器及常驻文档消费者。文档接收、来源元数据更新、预览/采用/重试、旧索引清理与正常批次续建已接入事务受理，仍在验证。已运行公共代码 `compileall` 与 `alembic upgrade a91b3c7d5e20:head --sql`，均通过（只生成 SQL，不连接数据库）。T01 至 T17 的行为验证及前端实施尚未收口，阶段复选框保持未完成。

开发中按改动执行最小充分验证，阶段结束再做必要回归，不反复重跑相同代码已通过的检查。后端验证入口遵循 [后端 README 的测试说明](../../backend/README.md#测试)，前端遵循 [前端 README 的验证说明](../../frontend/README.md#验证)；OpenAPI 类型必须重新生成，不手改生成物。前端最终构建已包含类型检查，同一份代码无需再重复单独执行。

每次进度更新记录已完成的 P 项、覆盖的 T 项、实际验证环境与结果、未运行原因及剩余问题。结果未知、环境不可用或尚未得到真实服务操作授权时，保留对应待办，不以离线测试替代生产可靠性结论。

重构交付需要功能、契约、迁移、必要验证与文档一致；真实环境切换和未解决的恢复问题仍需明确列出。施工完成、验证通过后，按仓库规则先与老板确认，再删除本 spec，并在交付说明中报告实际删除的 spec 路径。实施和待验证期间保留本文。
