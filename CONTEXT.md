# 新闻语义检索

本仓库唯一的业务领域：把新闻正文切成可检索片段并向量化入库，供用户按语义搜索、打开全文阅读，
或者让模型带着检索结果回答提问。
`frontend/` 和 `backend/` 是同一套词汇的两个运行时，所以术语只在这里定义一次。

本文只是术语表：一个词条一句定义，只说「是什么」。实现细节、需求和待办不写在这里，决策和取舍写进
[`docs/adr/`](docs/adr/)。

## 语言

**Document**：
一个来源下的一篇逻辑新闻，PostgreSQL 是它的事实来源。
_Avoid_：文章、新闻条目；也不要和 LangChain 的 `Document` 类混用，后者是后端索引管道里的中间对象，不是业务概念。

**Chunk**：
Document 正文切分后的一个可检索片段，Qdrant 里一个 Point 对应一个 Chunk。
_Avoid_：段落、切片；面向用户的界面文案可以说「片段」，代码、文档和讨论里用 Chunk。

**content_hash**：
一份正文的 SHA-256。检索结果里的它标识 Qdrant 已索引的那个正文版本，全文详情里的它标识 PostgreSQL 当前的正文版本；两者不等就表示正文在被索引之后又改过。
_Avoid_：正文 hash（不带限定词时说不清是哪个版本）。

**revision**：
Document 的业务版本号，任何可索引字段发生变化都会递增。对外只暴露这一个，含义是「业务记录当前是第几版」，不代表索引已经跟上。
_Avoid_：索引版本、版本；后端另有「最近一次成功写入索引对应的版本」，和本词条不是同一个数。

**score / bestScore**：
score 是单个 Chunk 与 query 的原始 Cosine 相似度，可以是负数；bestScore 是一篇 Document 在本次检索命中里的最高 score。两者都不是概率、百分比或置信度。
_Avoid_：相关度百分比、匹配率、置信度。

**Agent**：
一次受限循环：模型自己决定要不要调 Tool、拿到结果后再决定继续调还是收尾作答。
_Avoid_：机器人、智能体、AI；也不要用 Agent 指整个后端服务（那是 `backend/` 这个运行时）。

**Tool**：
暴露给模型的一个带名字和参数 schema 的函数，模型只能通过它读数据。参数 schema 的字段说明会进入模型上下文，所以它是提示词的一部分，不只是校验。
_Avoid_：函数、接口、插件、能力。

**会话（thread）**：
共享同一串历史消息的多轮对话，checkpointer 按它存取历史。
_Avoid_：session（会和登录会话撞名，那个指 Cookie 背后的登录态）、对话、上下文。

**运行（run）**：
一次用户提问到最终回答之间的整段执行，中间可能有多次模型调用和多次 Tool 调用。中间件的 `run_limit` 限的是它，`thread_limit` 限的是整个会话。
_Avoid_：轮、回合、step；说「第几轮」时分不清指一次运行还是一次模型调用。
